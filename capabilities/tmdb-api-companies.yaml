!!omap
- naftiko: 1.0.0-alpha2
- info: !!omap
  - label: TMDB API — Companies
  - description: |-
      TMDB — Companies. 3 operations. Lead operation: TMDB Companies — Details. Self-contained Naftiko capability covering one TMDB business surface.
  - tags:
    - TMDB
    - Companies
  - created: '2026-05-30'
  - modified: '2026-05-30'
- binds:
  - !!omap
    - namespace: env
    - keys: !!omap
      - TMDB_BEARER_TOKEN: TMDB_BEARER_TOKEN
- capability: !!omap
  - consumes:
    - !!omap
      - type: http
      - namespace: tmdb-api-companies
      - baseUri: https://api.themoviedb.org
      - description: TMDB API — Companies business capability. Self-contained, no shared references.
      - authentication: !!omap
        - type: bearer
        - token: '{{env.TMDB_BEARER_TOKEN}}'
      - resources:
        - !!omap
          - name: 3-company-company-id
          - path: /3/company/{company_id}
          - operations:
            - !!omap
              - name: company-details
              - method: GET
              - description: TMDB Companies — Details
              - inputParameters:
                - !!omap
                  - name: company_id
                  - in: path
                  - type: integer
                  - required: true
                  - description: company_id parameter.
              - outputRawFormat: json
              - outputParameters:
                - !!omap
                  - name: result
                  - type: object
                  - value: $.
        - !!omap
          - name: 3-company-company-id-alternative-names
          - path: /3/company/{company_id}/alternative_names
          - operations:
            - !!omap
              - name: company-alternative-names
              - method: GET
              - description: TMDB Companies — Alternative Names
              - inputParameters:
                - !!omap
                  - name: company_id
                  - in: path
                  - type: integer
                  - required: true
                  - description: company_id parameter.
              - outputRawFormat: json
              - outputParameters:
                - !!omap
                  - name: result
                  - type: object
                  - value: $.
        - !!omap
          - name: 3-company-company-id-images
          - path: /3/company/{company_id}/images
          - operations:
            - !!omap
              - name: company-images
              - method: GET
              - description: TMDB Companies — Images
              - inputParameters:
                - !!omap
                  - name: company_id
                  - in: path
                  - type: integer
                  - required: true
                  - description: company_id parameter.
              - outputRawFormat: json
              - outputParameters:
                - !!omap
                  - name: result
                  - type: object
                  - value: $.
  - exposes:
    - !!omap
      - type: rest
      - namespace: tmdb-api-companies-rest
      - port: 8080
      - description: REST adapter for TMDB API — Companies. One Spectral-compliant resource per consumed operation, prefixed with /v1.
      - resources:
        - !!omap
          - path: /v1/company/{company_id}
          - name: company-company-id
          - description: REST surface for /3/company/{company_id}.
          - operations:
            - !!omap
              - method: GET
              - name: company-details
              - description: TMDB Companies — Details
              - call: tmdb-api-companies.company-details
              - with: !!omap
                - company_id: rest.company_id
              - outputParameters:
                - !!omap
                  - type: object
                  - mapping: $.
        - !!omap
          - path: /v1/company/{company_id}/alternative_names
          - name: company-company-id-alternative-names
          - description: REST surface for /3/company/{company_id}/alternative_names.
          - operations:
            - !!omap
              - method: GET
              - name: company-alternative-names
              - description: TMDB Companies — Alternative Names
              - call: tmdb-api-companies.company-alternative-names
              - with: !!omap
                - company_id: rest.company_id
              - outputParameters:
                - !!omap
                  - type: object
                  - mapping: $.
        - !!omap
          - path: /v1/company/{company_id}/images
          - name: company-company-id-images
          - description: REST surface for /3/company/{company_id}/images.
          - operations:
            - !!omap
              - method: GET
              - name: company-images
              - description: TMDB Companies — Images
              - call: tmdb-api-companies.company-images
              - with: !!omap
                - company_id: rest.company_id
              - outputParameters:
                - !!omap
                  - type: object
                  - mapping: $.
    - !!omap
      - type: mcp
      - namespace: tmdb-api-companies-mcp
      - port: 9090
      - transport: http
      - description: MCP adapter for TMDB API — Companies. One tool per consumed operation, routed inline through this capability's consumes block.
      - tools:
        - !!omap
          - name: details
          - description: TMDB Companies — Details
          - hints: !!omap
            - readOnly: true
            - destructive: false
            - idempotent: true
          - call: tmdb-api-companies.company-details
          - with: !!omap
            - company_id: tools.company_id
          - outputParameters:
            - !!omap
              - type: object
              - mapping: $.
        - !!omap
          - name: alternative-names
          - description: TMDB Companies — Alternative Names
          - hints: !!omap
            - readOnly: true
            - destructive: false
            - idempotent: true
          - call: tmdb-api-companies.company-alternative-names
          - with: !!omap
            - company_id: tools.company_id
          - outputParameters:
            - !!omap
              - type: object
              - mapping: $.
        - !!omap
          - name: images
          - description: TMDB Companies — Images
          - hints: !!omap
            - readOnly: true
            - destructive: false
            - idempotent: true
          - call: tmdb-api-companies.company-images
          - with: !!omap
            - company_id: tools.company_id
          - outputParameters:
            - !!omap
              - type: object
              - mapping: $.
