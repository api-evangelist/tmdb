!!omap
- naftiko: 1.0.0-alpha2
- info: !!omap
  - label: TMDB API — Networks
  - description: |-
      TMDB — Networks. 3 operations. Lead operation: TMDB Networks — Details. Self-contained Naftiko capability covering one TMDB business surface.
  - tags:
    - TMDB
    - Networks
  - created: '2026-05-30'
  - modified: '2026-05-30'
- binds:
  - !!omap
    - namespace: env
    - keys: !!omap
      - TMDB_BEARER_TOKEN: TMDB_BEARER_TOKEN
- capability: !!omap
  - consumes:
    - !!omap
      - type: http
      - namespace: tmdb-api-networks
      - baseUri: https://api.themoviedb.org
      - description: TMDB API — Networks business capability. Self-contained, no shared references.
      - authentication: !!omap
        - type: bearer
        - token: '{{env.TMDB_BEARER_TOKEN}}'
      - resources:
        - !!omap
          - name: 3-network-network-id
          - path: /3/network/{network_id}
          - operations:
            - !!omap
              - name: network-details
              - method: GET
              - description: TMDB Networks — Details
              - inputParameters:
                - !!omap
                  - name: network_id
                  - in: path
                  - type: integer
                  - required: true
                  - description: network_id parameter.
              - outputRawFormat: json
              - outputParameters:
                - !!omap
                  - name: result
                  - type: object
                  - value: $.
        - !!omap
          - name: 3-network-network-id-alternative-names
          - path: /3/network/{network_id}/alternative_names
          - operations:
            - !!omap
              - name: details-copy
              - method: GET
              - description: TMDB Networks — Alternative Names
              - inputParameters:
                - !!omap
                  - name: network_id
                  - in: path
                  - type: integer
                  - required: true
                  - description: network_id parameter.
              - outputRawFormat: json
              - outputParameters:
                - !!omap
                  - name: result
                  - type: object
                  - value: $.
        - !!omap
          - name: 3-network-network-id-images
          - path: /3/network/{network_id}/images
          - operations:
            - !!omap
              - name: alternative-names-copy
              - method: GET
              - description: TMDB Networks — Images
              - inputParameters:
                - !!omap
                  - name: network_id
                  - in: path
                  - type: integer
                  - required: true
                  - description: network_id parameter.
              - outputRawFormat: json
              - outputParameters:
                - !!omap
                  - name: result
                  - type: object
                  - value: $.
  - exposes:
    - !!omap
      - type: rest
      - namespace: tmdb-api-networks-rest
      - port: 8080
      - description: REST adapter for TMDB API — Networks. One Spectral-compliant resource per consumed operation, prefixed with /v1.
      - resources:
        - !!omap
          - path: /v1/network/{network_id}
          - name: network-network-id
          - description: REST surface for /3/network/{network_id}.
          - operations:
            - !!omap
              - method: GET
              - name: network-details
              - description: TMDB Networks — Details
              - call: tmdb-api-networks.network-details
              - with: !!omap
                - network_id: rest.network_id
              - outputParameters:
                - !!omap
                  - type: object
                  - mapping: $.
        - !!omap
          - path: /v1/network/{network_id}/alternative_names
          - name: network-network-id-alternative-names
          - description: REST surface for /3/network/{network_id}/alternative_names.
          - operations:
            - !!omap
              - method: GET
              - name: details-copy
              - description: TMDB Networks — Alternative Names
              - call: tmdb-api-networks.details-copy
              - with: !!omap
                - network_id: rest.network_id
              - outputParameters:
                - !!omap
                  - type: object
                  - mapping: $.
        - !!omap
          - path: /v1/network/{network_id}/images
          - name: network-network-id-images
          - description: REST surface for /3/network/{network_id}/images.
          - operations:
            - !!omap
              - method: GET
              - name: alternative-names-copy
              - description: TMDB Networks — Images
              - call: tmdb-api-networks.alternative-names-copy
              - with: !!omap
                - network_id: rest.network_id
              - outputParameters:
                - !!omap
                  - type: object
                  - mapping: $.
    - !!omap
      - type: mcp
      - namespace: tmdb-api-networks-mcp
      - port: 9090
      - transport: http
      - description: MCP adapter for TMDB API — Networks. One tool per consumed operation, routed inline through this capability's consumes block.
      - tools:
        - !!omap
          - name: details
          - description: TMDB Networks — Details
          - hints: !!omap
            - readOnly: true
            - destructive: false
            - idempotent: true
          - call: tmdb-api-networks.network-details
          - with: !!omap
            - network_id: tools.network_id
          - outputParameters:
            - !!omap
              - type: object
              - mapping: $.
        - !!omap
          - name: alternative-names
          - description: TMDB Networks — Alternative Names
          - hints: !!omap
            - readOnly: true
            - destructive: false
            - idempotent: true
          - call: tmdb-api-networks.details-copy
          - with: !!omap
            - network_id: tools.network_id
          - outputParameters:
            - !!omap
              - type: object
              - mapping: $.
        - !!omap
          - name: images
          - description: TMDB Networks — Images
          - hints: !!omap
            - readOnly: true
            - destructive: false
            - idempotent: true
          - call: tmdb-api-networks.alternative-names-copy
          - with: !!omap
            - network_id: tools.network_id
          - outputParameters:
            - !!omap
              - type: object
              - mapping: $.
